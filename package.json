{
  "name": "thala-indexer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "sentio test",
    "gen": "sentio gen",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "@sentio/sdk": "^2.8.0"
  },
  "devDependencies": {
    "@sentio/cli": "^2.4.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.0",
    "typescript": "^4.9.5"
  }
}
