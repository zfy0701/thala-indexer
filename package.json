{
  "name": "thala-processor",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "gen": "sentio gen",
    "build": "sentio build",
    "upload": "sentio upload",
    "postinstall": "sentio gen",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "@sentio/sdk": "^1.35.0"
  },
  "devDependencies": {
    "@types/jest": "^29.2.3",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.0",
    "typescript": "^4.9.3"
  }
}
